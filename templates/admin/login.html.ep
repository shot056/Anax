<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="utf-8">
    <title>ログイン </title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="">
    <meta name="author" content="">

    <!-- Le styles -->

    <link href="<%= get_path('/static/css/bootstrap.min.css') %>" rel="stylesheet">
    <style type="text/css">
      body {
        padding-top: 40px;
        padding-bottom: 40px;
        background-color: #f5f5f5;
      }

      .form-signin {
        max-width: 300px;
        padding: 19px 29px 29px;
        margin: 0 auto 20px;
        background-color: #fff;
        border: 1px solid #e5e5e5;
        -webkit-border-radius: 5px;
           -moz-border-radius: 5px;
                border-radius: 5px;
        -webkit-box-shadow: 0 1px 2px rgba(0,0,0,.05);
           -moz-box-shadow: 0 1px 2px rgba(0,0,0,.05);
                box-shadow: 0 1px 2px rgba(0,0,0,.05);
      }
      .form-signin .form-signin-heading,
      .form-signin .checkbox {
        margin-bottom: 10px;
      }
      .form-signin input[type="text"],
      .form-signin input[type="password"] {
        font-size: 16px;
        height: auto;
        margin-bottom: 15px;
        padding: 7px 9px;
      }

    </style>
    <link href="<%= get_path('/static/css/bootstrap-responsive.css') %>" rel="stylesheet">

    <!-- HTML5 shim, for IE6-8 support of HTML5 elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->

    <!-- Fav and touch icons -->
    <link rel="shortcut icon" href="<%= get_path('/static/ico/favicon.ico') %>">

    <link rel="apple-touch-icon-precomposed" sizes="144x144" href="<%= get_path('/static/ico/apple-touch-icon-144-precomposed.png') %>">
    <link rel="apple-touch-icon-precomposed" sizes="114x114" href="<%= get_path('/static/ico/apple-touch-icon-114-precomposed.png') %>">
    <link rel="apple-touch-icon-precomposed" sizes="72x72" href="<%= get_path('/static/ico/apple-touch-icon-72-precomposed.png') %>">
    <link rel="apple-touch-icon-precomposed" href="<%= get_path('/static/ico/apple-touch-icon-57-precomposed.png') %>">
  </head>

  <body>

    <div class="container">

      <form class="form-signin" action="<%= get_path('/admin/login') %>" method="post">
        <h2 class="form-signin-heading">Anax Web Form</h2>
        % if( stash 'failure' ) {
        <p class="text-danger">ログインID もしくはパスワードが違います。</p>
        % }
        <input type="text" class="form-control" placeholder="Login ID" name="login_id" />
        <input type="password" class="form-control" placeholder="Password" name="password" />
        <button class="btn btn-lg btn-primary" type="submit">ログイン</button>
      </form>

    </div> <!-- /container -->

    <!-- Le javascript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <script src="<%= get_path('/static/js/jquery-1.8.2.js') %>"></script>
    <script src="<%= get_path('/static/js/jquery-ui-1.9.1.custom.min.js') %>"></script>
    <script src="<%= get_path('/static/js/bootstrap.min.js') %>"></script>

  </body>

</html>
